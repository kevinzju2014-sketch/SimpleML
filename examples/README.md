# 示例（均可用）

请先在 Grasshopper 运行 **环境体检**，确认 PASS。

| 文件 | 用途 |
|------|------|
| `01_classification_iris.md` | 分类：iris → 分割 → 智能训练 → 预测 → 评估 |
| `02_clustering_color.md` | 聚类：make_blobs → 智能训练 → 一键上色 |
| `03_regression.md` | 回归：diabetes → 分割 → 智能训练 → 评估 |
| `quickstart_all.py` | 不依赖 GH 的三任务冒烟脚本 |

```bash
python examples/quickstart_all.py
```
